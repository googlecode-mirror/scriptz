#summary how to use sqlite database
#labels Phase-SQLite

= How to use Sqlite =

== Create Tables ==

{{{
sqlite> CREATE TABLE tbl2 (
   ...>   f1 varchar(30) primary key,
   ...>   f2 text,
   ...>   f3 real
   ...> );
sqlite>
}}}